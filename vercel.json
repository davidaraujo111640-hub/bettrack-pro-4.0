{
  "buildCommand": "chmod +x node_modules/.bin/vite node_modules/.bin/esbuild && npx vite build && npx esbuild server.ts --bundle --platform=node --format=cjs --packages=external --sourcemap --outfile=dist/server.cjs",
  "outputDirectory": "dist",
  "functions": {
    "api/extract-bet.js": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/extract-bet",
      "destination": "/api/extract-bet"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
